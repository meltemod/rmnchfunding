% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rescale01.R
\name{rescale01}
\alias{rescale01}
\title{Rescale a numeric vector to the unit interval}
\usage{
rescale01(x, na.rm = TRUE)
}
\arguments{
\item{x}{A numeric vector.}

\item{na.rm}{Logical. Should \code{NA} values be ignored when finding the
range? If \code{FALSE}, any \code{NA} in \code{x} makes the whole result \code{NA}.}
}
\value{
A numeric vector the same length as \code{x}, with values in \verb{[0, 1]}.
A constant \code{x} has no range to scale by and returns all zeroes.
}
\description{
Linearly maps \code{x} so that its minimum becomes 0 and its maximum becomes 1.
}
\details{
This is an EXAMPLE function, present so the package is checkable, testable
and documented from the first commit. Delete it — along with
\code{tests/testthat/test-rescale01.R} and \code{man/rescale01.Rd} — once you have
real code, and remove \code{export(rescale01)} from NAMESPACE by re-running
\code{devtools::document()}.
}
\examples{
rescale01(c(2, 4, 6, 8))
rescale01(c(1, NA, 3))
}

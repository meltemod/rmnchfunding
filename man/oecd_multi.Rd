% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oecd_multi.R
\name{oecd_multi}
\alias{oecd_multi}
\title{Fetch a provider's core contributions to multilateral agencies}
\usage{
oecd_multi(
  donor,
  years,
  agencies = NULL,
  prices = c("constant", "current"),
  base = NULL,
  measure = "10",
  flow_type = "D",
  quiet = FALSE
)
}
\arguments{
\item{donor}{OECD donor code(s), e.g. \code{"USA"}.}

\item{years}{Integer vector of years to fetch.}

\item{agencies}{Agency names as they appear in \link{agency_weights}. Defaults to
all eleven. Names are validated against \link{agency_channels}; an unknown name
is an error rather than an empty result.}

\item{prices}{Either \code{"constant"} (needs \code{base}) or \code{"current"}.}

\item{base}{Base year for \code{prices = "constant"}.}

\item{measure}{OECD measure code. Defaults to \code{"10"}, core contributions.
Pass \code{"20"} for earmarked contributions through the agency, but see
Details before combining them.}

\item{flow_type}{Defaults to \code{"D"}, disbursements.}

\item{quiet}{Set \code{TRUE} to suppress the summary message.}
}
\value{
A tibble of one row per donor, agency and year:
\describe{
\item{donor, donor_name}{Provider code and label.}
\item{agency}{Agency name as used by \link{agency_weights}, so the two join
directly.}
\item{year}{Calendar year.}
\item{value}{Core contribution in millions of USD, in the prices given by
the \code{prices} and \code{base_year} attributes.}
\item{n_channels}{How many OECD channel codes were summed into this row.
\code{1} for every agency except the World Health Organisation, which can be
\code{2} — it is \code{1} where a donor reported under only one of the WHO's two
core channels in that year.}
}
with attributes \code{prices}, \code{base_year}, \code{measure} and \code{fetched_on}.

As with \code{\link[=oecd_crs]{oecd_crs()}}, a donor with no core contributions in the requested
years returns 0 rows with the same columns, and warns, rather than erroring.
}
\description{
Pulls a provider's use of the multilateral system from the OECD table of the
same name, restricted to the agencies the Muskoka method weights. The result
is what \code{muskoka()} applies \link{agency_weights} to, giving the multilateral half
of an estimate; \code{\link[=oecd_crs]{oecd_crs()}} gives the bilateral half.
}
\details{
Only \strong{core} contributions are returned by default. OECD splits this table
into core contributions to an agency (\code{measure = "10"}) and earmarked
contributions channelled through it (\code{"20"}). The Muskoka weights are
defined against core contributions, and adding the earmarked ones would
double-count spending that CRS already reports bilaterally by purpose code.

Agencies are identified by OECD channel code, never by name — see
\link{agency_channels} for why, and for the World Health Organisation's two
codes, which are summed.
}
\section{Prices}{

As \code{\link[=oecd_crs]{oecd_crs()}}. \code{prices = "current"} leaves each year in its own prices;
\code{prices = "constant"} requires an explicit \code{base} year and deflates with
OECD's per-donor deflators. Use the same setting for both fetchers, or the
bilateral and multilateral halves of an estimate will not be comparable.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
oecd_multi("USA", years = 2022:2024, prices = "constant", base = 2023)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=oecd_crs]{oecd_crs()}}, \link{agency_channels}, \link{agency_weights}.
}

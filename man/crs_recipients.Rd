% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{crs_recipients}
\alias{crs_recipients}
\title{CRS recipient codes: which are aggregates, which are countries}
\format{
A data frame with 290 rows and 7 columns:
\describe{
\item{recipient_code}{OECD recipient code. Unique.}
\item{recipient_name}{Official OECD name, from codelist \code{CL_AREA_ORG}.
Bundled so that a recipient absent from a donor's data can still be
labelled — see the \code{complete} argument of \code{\link[=crs_classify]{crs_classify()}}.}
\item{n_children}{Most children the code has in any grouping.}
\item{n_appearances}{How many groupings the code appears in.}
\item{min_depth}{Shallowest depth at which it appears.}
\item{is_aggregate}{\code{TRUE} if \code{n_children > 0}. 39 codes are aggregates
and 251 are leaves. This is what de-duplication keys on.}
\item{is_unallocated}{\code{TRUE} for OECD's 28 \verb{_X} buckets, which hold
spending not attributable to a country. See Leaves are not countries.}
}
}
\source{
Structure from OECD hierarchical codelist \code{HCL_DACRECIPIENTS}
version 1.5; names from codelist \code{CL_AREA_ORG} version 1.6, which the
hierarchy declares as its source. Retrieved 2026-07-30. Rebuild with
\code{data-raw/crs_recipients.R}.
}
\usage{
crs_recipients
}
\description{
The OECD recipient codelist is hierarchical, and this records the part of
that structure \code{\link[=oecd_crs]{oecd_crs()}} needs: whether a code stands for a group of
recipients or for a single one.
}
\details{
A CRS query with the recipient dimension left open returns aggregates and
individual countries as \strong{sibling rows}. "Developing countries", "Africa"
and "Kenya" all come back together, so summing the rows counts Kenya once
as itself, again in its region, again in its continent and again in the
global total. Nothing in the response marks which rows are aggregates, and
the resulting total can be several times the true figure. This dataset is
how \code{\link[=oecd_crs]{oecd_crs()}} strips them, without a network call on every query.
}
\section{The hierarchy is not a tree}{

The top level holds overlapping \emph{analytical groupings} rather than one
nesting: \code{DPGC} (developing countries) sits beside \code{LLDC} (landlocked least
developed), \code{SIDS} (small island states), \code{FSCAC} (fragile contexts), the
World Bank income groups (\code{OLICWB}, \code{LMICWB}, \code{UMICWB}, \code{HICSWB}) and
others. A country belongs to several at once, so codes have many parents
and recur at different depths — one code appears six times.

Two consequences shape this table. It is keyed by \strong{distinct code}, since a
walk emitting one row per visit yields 863 rows for 290 codes. And
\code{is_aggregate} means "has children \emph{anywhere} in the hierarchy", because a
code that is a leaf of one grouping may parent members of another, and must
still not be summed alongside them.
}

\section{Leaves are not countries}{

OECD reports the part of a donor's spending it cannot attribute to any one
country in \verb{_X} buckets: \code{DPGC_X} "Developing countries unspecified",
\code{F6_X} "Sub-Saharan Africa unspecified", and so on for each region. These
have no members, so they are leaves — correctly, because unlike a region
they do not overlap the countries beside them and can safely be summed
alongside them.

They are nonetheless not countries, and the distinction is large rather than
pedantic: for the United States in 2022 they are over 40\% of the
disbursements this package fetches. \code{is_unallocated} marks them so a caller
can sum by country without them and sum a donor total with them.

The flag is independent of \code{is_aggregate}. Two \verb{_X} codes, \code{INC_X} and
\code{INCWB_X}, group the countries of unspecified income classification and so
do have members; they are aggregates as well as unallocated.
}

\seealso{
\code{\link[=oecd_crs]{oecd_crs()}}, whose \code{recipients} argument uses this.
}
\keyword{datasets}

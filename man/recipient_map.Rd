% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipient_map.R
\name{recipient_map}
\alias{recipient_map}
\title{How OECD recipients map to the source data, and where borrowed weights come
from}
\usage{
recipient_map(purpose_code = NULL, imputed_only = FALSE, year = NULL)
}
\arguments{
\item{purpose_code}{Optionally one CRS purpose code. Given one, the result
carries a single \code{source} column for that code; given \code{NULL} (default),
one \verb{source_<code>} column per code. Must be one of the four codes whose
weight varies by recipient: \code{"12262"}, \code{"12263"}, \code{"13040"}, \code{"51010"}.}

\item{imputed_only}{Set \code{TRUE} to keep only recipients whose weight is
borrowed — for \code{purpose_code}, or for any code when that is \code{NULL}.}

\item{year}{Optional single year. The provenance of a weight can change
across the series — a country whose own data begins partway through is
substituted before that point and observed after it — so with \code{year}
unset a recipient whose source changes is reported as \code{"mixed"}. Set it
to resolve that to the source actually used in that year.}
}
\value{
A tibble with one row per OECD recipient:
\describe{
\item{recipient_code, recipient_name}{OECD identifiers.}
\item{iso3, wb_name}{World Bank identifiers, \code{NA} if absent.}
\item{gbd_location_name}{IHME GBD location name, \code{NA} if absent.}
\item{continent, region, subregion}{OECD DAC hierarchy codes.}
\item{continent_name, region_name, subregion_name}{The same as names.
\code{NA} where that level does not exist.}
\item{has_worldbank_data, has_gbd_data}{Whether the recipient appears in
each source.}
\item{no_data_reason}{Why a recipient is absent from the World Bank list,
where that is known.}
\item{source or source_\if{html}{\out{<code>}}}{\code{"own"}, or which geographic group the
weight was borrowed from.}
}
}
\description{
Returns the crosswalk the package uses internally: each OECD recipient
alongside its World Bank and IHME GBD identifiers, its position in the OECD
DAC geographic hierarchy, and — for the four weights that vary by recipient
and year — whether its weight is its own or borrowed from a geographic
group.
}
\details{
This joins two things that are otherwise separate. \link{recipient_crosswalk} has
the identifiers and geography but nothing about imputation;
\link{rmnch_recipient_weights} records the imputation but repeats every recipient
once per year and purpose code. What a reader checking the method usually
wants is one row per recipient, which is what this returns.

The same content ships as a plain file for use outside R:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{read.csv(system.file("extdata", "recipient_crosswalk.csv",
                     package = "rmnchfunding"))
}\if{html}{\out{</div>}}
}
\section{Three naming systems}{

No two of the sources spell countries the same way, and none is reachable
from another by plain string matching — Cote d'Ivoire differs between all
three in both its accent and its apostrophe. That is why the mapping is
stored rather than computed at the point of use.

\code{iso3} is \code{NA} where the World Bank has no record, and \code{gbd_location_name}
is \code{NA} where GBD does not cover the recipient separately. Those are the
recipients whose weights are borrowed.
}

\section{What "borrowed" means}{

A recipient absent from the source data takes the unweighted mean of the
weights of the recipients in its geographic group, for the same year, trying
the narrowest group first: subregion, then region, then continent. The
\verb{source_*} columns record which was used.

The geography is the \strong{OECD DAC} hierarchy, not UN M49 and not the World
Bank's regions; see \link{recipient_crosswalk} for how they differ and why it
matters. Note that the hierarchy is ragged, so \code{region_name} is \code{NA} for the
fifteen European recipients, whose continent is not subdivided.
}

\examples{
# the whole crosswalk
recipient_map()

# which recipients borrow a malaria weight, and from where
recipient_map("12262", imputed_only = TRUE)[
  c("recipient_name", "continent_name", "source")
]

# look up how one recipient is identified across the three sources
m <- recipient_map()
m[m$recipient_code == "CIV",
  c("recipient_name", "wb_name", "gbd_location_name")]

}
\seealso{
\link{recipient_crosswalk} and \link{rmnch_recipient_weights} for the
underlying data.
}

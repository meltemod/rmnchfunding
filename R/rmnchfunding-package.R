#' @keywords internal
"_PACKAGE"

# Package-level documentation. `_PACKAGE` tells roxygen2 to build the
# `?rmnchfunding` help page from the DESCRIPTION file, so
# title, description and author never drift between the two.
#
# Imports from other packages are declared BOTH here (`@importFrom`) and in
# DESCRIPTION. The two do different jobs and both are required: DESCRIPTION
# makes R install the dependency, this file makes the function visible inside
# the package namespace.

## usethis namespace: start
## usethis namespace: end
NULL
